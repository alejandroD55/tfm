# =============================================================================
# requirements_local.txt — Dependencias para local_backtest_runner.py
#                          + FastAPI local (run_api_local.sh)
# =============================================================================
# Instalar con:
#   pip install -r requirements_local.txt --break-system-packages
#
# Ollama NO va aquí — es un proceso separado.
# Instalación: https://ollama.com/download
# Modelo:       ollama pull llama3.2:3b
# =============================================================================

# ── Datos de mercado ──────────────────────────────────────────────────────────
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0

# ── Indicadores técnicos ──────────────────────────────────────────────────────
# pandas-ta-classic es el fork mantenido, compatible con Python 3.11+
pandas-ta-classic

# ── FinBERT local (reemplaza HuggingFace Inference API) ───────────────────────
transformers>=4.30.0
# PyTorch (elige solo uno según tu hardware):
torch>=2.0.0                        # CPU + GPU (CUDA/MPS)
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu  # solo CPU (más ligero)

# ── Red Bayesiana (idéntico a Lambda) ─────────────────────────────────────────
pgmpy>=0.1.24
scipy
networkx
joblib

# ── Base de datos ─────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.0              # PostgreSQL local (docker-compose)
pymongo[srv]>=4.6.0                 # MongoDB Atlas opcional

# ── HTTP / APIs ───────────────────────────────────────────────────────────────
requests>=2.28.0
newsapi-python>=0.2.7               # macro context (opcional)

# ── Utilidades ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.65.0

# ── Visualización (opcional, para gráficas de backtesting) ───────────────────
matplotlib>=3.7.0

# ── FastAPI local (run_api_local.sh) — mismo stack que el pod API en EKS ─────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
